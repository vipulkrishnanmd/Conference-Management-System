// This is where the magic happens
// Add this event to your JS to enable active states on all of your elements. This can be a bit slow on huge pages so it might be worth restricting it to certain elements instead of document
document.addEventListener("touchstart", function(){}, true)